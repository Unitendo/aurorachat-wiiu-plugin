# libs

All the files were taken from https://github.com/wiiu-env/NotificationModule/tree/main/src/gui

* Changes were made to the files to make them function with the plugin.
* OpenGL Mathematics (GLM) is no longer a dependency.